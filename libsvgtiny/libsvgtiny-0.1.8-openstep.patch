diff --git a/src/svgtiny.c b/src/svgtiny.c
index ee0c59c..3a7e72d 100644
--- a/src/svgtiny.c
+++ b/src/svgtiny.c
@@ -20,6 +20,26 @@
 #include "svgtiny.h"
 #include "svgtiny_internal.h"
 
+#ifdef NeXT
+static char *svgtiny_strdup(const char *s)
+{
+	size_t len = strlen(s) + 1;
+	char *copy = malloc(len);
+	if (copy != NULL)
+		memcpy(copy, s, len);
+	return copy;
+}
+#define strdup svgtiny_strdup
+#ifndef fabsf
+#define fabsf(x) ((float) fabs((double) (x)))
+#endif
+#ifndef lroundf
+#define lroundf(x) \
+	((long) ((x) >= 0.0f ? floor((double) (x) + 0.5) : \
+	ceil((double) (x) - 0.5)))
+#endif
+#endif
+
 /* Source file generated by `gperf`. */
 #include "autogenerated_colors.c"
 
diff --git a/src/svgtiny_gradient.c b/src/svgtiny_gradient.c
index 4b327dd..722dec4 100644
--- a/src/svgtiny_gradient.c
+++ b/src/svgtiny_gradient.c
@@ -14,6 +14,18 @@
 #include "svgtiny.h"
 #include "svgtiny_internal.h"
 
+#ifdef NeXT
+#ifndef fabsf
+#define fabsf(x) ((float) fabs((double) (x)))
+#endif
+#ifndef ceilf
+#define ceilf(x) ((float) ceil((double) (x)))
+#endif
+#ifndef isnan
+#define isnan(x) ((x) != (x))
+#endif
+#endif
+
 #undef GRADIENT_DEBUG
 
 static svgtiny_code svgtiny_parse_linear_gradient(dom_element *linear,
@@ -780,4 +792,3 @@ void svgtiny_invert_matrix(float *m, float *inv)
 	inv[4] = (m[2]*m[5] - m[3]*m[4]) / determinant;
 	inv[5] = (m[1]*m[4] - m[0]*m[5]) / determinant;
 }
-
